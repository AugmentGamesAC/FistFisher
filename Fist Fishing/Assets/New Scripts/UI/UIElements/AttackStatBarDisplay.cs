using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AttackStatBarDisplay : MonoBehaviour
{
    public ProgressBarUpdater PowerStatBar;
    public ProgressBarUpdater RangeStatBar;
    public ProgressBarUpdater OxygenStatBar;
    public ProgressBarUpdater MovementStatBar;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
