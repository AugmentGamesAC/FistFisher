using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/// <summary>
/// feature that we haven't got to.
/// The punchadex was to describe details about the fish, coral, colletables, biomes, etc. 
/// haing an actual game was more of a priority than an encyclopedia, howver.
/// </summary>
public class Punchadex : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
