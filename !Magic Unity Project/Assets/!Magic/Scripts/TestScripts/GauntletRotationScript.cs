using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GauntletRotationScript : MonoBehaviour
{

    public KeyCode m_rotateLeft = KeyCode.LeftArrow;
    public KeyCode m_rotateRight = KeyCode.RightArrow;
    public KeyCode m_rotateUp = KeyCode.UpArrow;
    public KeyCode m_rotateDown = KeyCode.DownArrow;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //if (Input.GetKeyDown(KeyCode.N))
    }
}
