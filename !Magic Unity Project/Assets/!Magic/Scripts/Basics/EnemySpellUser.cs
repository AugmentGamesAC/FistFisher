using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//this class is for enemies if they happen to have anything odd going on with their shield/mana/etc.
//as it stands, this is just a basic spell user by a different name
public class EnemySpellUser : BasicSpellUser
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
